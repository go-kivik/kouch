package config

const (
	// homeDir is the default directory where config is stored under the user's
	// home directory.
	homeDir = ".kouch"

	flagContext = "context"

	dynamicContextName = "$dynamic$"
)
