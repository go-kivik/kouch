// Package kouchio provides basic I/O primitives meant to suppliment the
// standard library's io package. kouchio should have no dependencies other than
// the Go standard library, to avoid import loops.
package kouchio
